{
    "name": "vimbo/api",
    "description": "vimbo — movie store showcase generator",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "fakerphp/faker": "^1.24"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
